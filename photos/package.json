{
  "name": "photo-portfolio",
  "version": "1.0.0",
  "description": "TypeScript-based photo portfolio viewer",
  "scripts": {
    "build": "tsc photos.ts --outFile photos.js --target ES2020 --lib ES2020,DOM,DOM.Iterable",
    "watch": "tsc photos.ts --outFile photos.js --target ES2020 --lib ES2020,DOM,DOM.Iterable --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

